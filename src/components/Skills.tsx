const Skills = () => {
    return (
        <section id="skills" className="section">
            <h2>Skills</h2>
            <p>Java, JavaScript, TypeScript, React, Node.js, HTML, CSS</p>
        </section>

    );
}

export default Skills;